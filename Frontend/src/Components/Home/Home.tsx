import Layout from "../Shared/Layout"

// This is represent Home page may be we can add more component on it as needed
const Home = () => {
  return (
    <div>
      <Layout />
    </div>
  )
}

export default Home